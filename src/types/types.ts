export type NavigationLink = {
  href: string;
  label: string;
};

export type FormStatus = {
  status: "error" | "success" | "";
  message: string;
};

export type PageNavigationLink = {
  link: string;
  label: string;
};
